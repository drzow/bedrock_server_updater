pub fn main() -> Result<(), String> {
/*
    // Unzip the package
    let directory = unzip_package_file(package_file);

    // Remove configuration files
    remove_conf_files(directory);

    // Zip the package
    let zip_file = zip_package(directory);
*/
    // Profit
    Ok(())
}
/*
fn unzip_package_file(package_file) -> DirRef {
    // TODO
}

fn remove_conf_files(directory) {
    // TODO
}

fn zip_package(directory) {
    // TODO
}
*/